# Solo tests web
mvn test -Dcucumber.filter.tags="@web"

# Solo tests API de Karate
mvn test -Dtest=KarateRunner

# Solo Cucumber (web)
mvn test -Dtest=CucumberRunner

# Todos juntos
mvn test

mvn dependency:resolve

mvn allure:report