# Solo tests web
mvn test -Dcucumber.filter.tags="@web"

# Solo tests API de Karate
mvn test -Dtest=KarateRunner

# Solo Cucumber (web)
mvn test -Dtest=CucumberRunner

#solo karate
mvn test -Dtest=KarateRunner

# Todos juntos
mvn test

mvn dependency:resolve

mvn allure:report

mvn test -Dtest=CucumberRunner -Dcucumber.filter.tags="@web" -Dcucumber.filter.name="Login con usuario invalido desde DataProvider"